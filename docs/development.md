# 开发与运行

## 常用操作

- 依赖、脚本和构建要求以 `package.json` 为准。
- 使用项目定义的开发命令启动，构建后再进行发布前验证。
- API 变更时同步检查 `src/api/` 类型、页面状态、权限指令和错误提示。

## 验证清单

- 页面路由、菜单显示、按钮禁用/提示符合授权模型；
- 表单校验、加载、空态和失败反馈完整；
- 高风险发布类操作有确认、冲突和失败状态；
- 不把模拟数据、测试账号或环境凭证写入业务文档。
